function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">BioWatch Frontend</p>
        <h1>Vite + React + TypeScript base is ready.</h1>
        <p>
          This starter page confirms the first ticket is complete: scaffold, linting, formatting,
          and local run setup.
        </p>
      </section>
    </main>
  );
}

export default App;
